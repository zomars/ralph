#!/bin/zsh
set -e
RALPH_HOME="$(cd "$(dirname "$(realpath "$0")")/.." && pwd)"

if [[ -z "$1" ]]; then
  echo "Usage: afk-claude <iterations>"
  exit 1
fi

source "$RALPH_HOME/lib/ralph-iter-loop.sh"
ralph_iter_loop "$1" "$RALPH_HOME/prompts/generic.md"
