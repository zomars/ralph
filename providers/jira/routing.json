{
  "statuses": ["To Do", "In Progress", "In Review", "Done"],
  "labels": [
    "needs-planning",
    "needs-tests",
    "tech-debt",
    "ralph-blocked",
    "ralph-failed",
    "needs-input",
    "documented"
  ],
  "global_label_excludes": ["needs-input"],
  "agents": {
    "planner": {
      "jql": "assignee = currentUser() AND ((description is EMPTY OR description ~ 'TODO') OR labels = 'needs-planning') AND labels not in ('needs-input', 'needs-tests', 'tech-debt', 'ralph-blocked', 'ralph-failed', 'documented') AND status in ('To Do', 'In Progress') ORDER BY createdDate DESC",
      "adds_labels": ["needs-input", "needs-tests"],
      "rules": {
        "status_in": ["To Do", "In Progress"],
        "labels_include": null,
        "labels_exclude": ["needs-input", "needs-tests", "tech-debt", "ralph-blocked", "ralph-failed", "documented"],
        "description_condition": "empty_or_todo_or_label_needs_planning"
      },
      "script": "bin/afk-planner",
      "prompt": "prompts/planner.md"
    },
    "implementer": {
      "jql": "assignee = currentUser() AND status in ('To Do', 'In Progress') AND (description is not EMPTY AND description !~ 'TODO') AND labels not in ('needs-tests', 'tech-debt', 'ralph-blocked', 'needs-planning', 'needs-input') ORDER BY priority DESC",
      "adds_labels": ["ralph-blocked", "ralph-failed"],
      "rules": {
        "status_in": ["To Do", "In Progress"],
        "labels_include": null,
        "labels_exclude": ["needs-tests", "tech-debt", "ralph-blocked", "needs-planning", "needs-input"],
        "description_condition": "not_empty_and_not_todo"
      },
      "script": "bin/afk-implementer",
      "prompt": "prompts/implementer.md"
    },
    "reviewer": {
      "jql": "assignee = currentUser() AND status = 'In Review' AND labels not in ('needs-planning', 'needs-tests', 'tech-debt', 'needs-input') ORDER BY priority DESC",
      "adds_labels": ["ralph-failed", "needs-tests", "tech-debt"],
      "rules": {
        "status_in": ["In Review"],
        "labels_include": null,
        "labels_exclude": ["needs-planning", "needs-tests", "tech-debt", "needs-input"],
        "description_condition": null
      },
      "script": "bin/afk-reviewer",
      "prompt": "prompts/reviewer.md"
    },
    "tester": {
      "jql": "assignee = currentUser() AND labels = 'needs-tests' AND labels not in ('needs-planning', 'needs-input') AND status != 'Done' ORDER BY priority DESC",
      "adds_labels": ["tech-debt"],
      "rules": {
        "status_in": ["To Do", "In Progress", "In Review"],
        "labels_include": ["needs-tests"],
        "labels_exclude": ["needs-planning", "needs-input"],
        "description_condition": null
      },
      "script": "bin/afk-tester",
      "prompt": "prompts/tester.md"
    },
    "refactor": {
      "jql": "assignee = currentUser() AND labels = 'tech-debt' AND labels not in ('needs-planning', 'needs-input', 'needs-tests') ORDER BY priority DESC",
      "adds_labels": [],
      "rules": {
        "status_in": ["To Do", "In Progress", "In Review", "Done"],
        "labels_include": ["tech-debt"],
        "labels_exclude": ["needs-planning", "needs-input", "needs-tests"],
        "description_condition": null
      },
      "script": "bin/afk-refactor",
      "prompt": "prompts/refactor.md"
    },
    "documenter": {
      "jql": "assignee = currentUser() AND status = 'Done' AND labels not in ('documented', 'tech-debt', 'needs-input') ORDER BY updated DESC",
      "adds_labels": ["documented"],
      "rules": {
        "status_in": ["Done"],
        "labels_include": null,
        "labels_exclude": ["documented", "tech-debt", "needs-input"],
        "description_condition": null
      },
      "script": "bin/afk-documenter",
      "prompt": "prompts/documenter.md"
    }
  }
}
